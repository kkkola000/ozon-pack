# Только для запуска тестов. На сервер не ставится: панели эти пакеты не нужны.
-r requirements.txt
pytest==9.1.1
# Чтением готового PDF проверяется лист возвратов: в нём должны оказаться
# и сами возвраты, и отметки сборщика (tests/test_returns_mark.py).
pypdf==6.16.2
